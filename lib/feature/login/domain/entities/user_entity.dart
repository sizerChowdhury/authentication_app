class User {
  String firstName, lastName, email, role, id;

  User({
    required this.firstName,
    required this.lastName,
    required this.email,
    required this.id,
    required this.role,
  });
}